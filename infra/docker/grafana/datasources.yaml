apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: True
    editable: true
    jsonData:
      httpMethod: POST
      # This helps with faster loading for high-resolution benchmarks
      timeInterval: "5s"